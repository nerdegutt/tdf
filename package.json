{
  "name": "tdf",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/prerender.mjs && node scripts/render-pdf.mjs",
    "build:fast": "vite build && node scripts/prerender.mjs",
    "build:hq": "PDF_HQ=1 npm run build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.18",
    "leaflet": "^1.9.4",
    "tailwindcss": "^4.1.18",
    "vite": "^7.3.1"
  },
  "devDependencies": {
    "puppeteer": "^24.42.0"
  }
}
